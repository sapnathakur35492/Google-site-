# Google Sites Automation Configuration

# SLUG_PREFIX: Just put the base name (e.g. 'dhr', 'santosh'). 
# The system will automatically add '-01', '-02' etc.
SLUG_PREFIX=ee

# Note: The system will automatically generate slugs like: {SLUG_PREFIX}-01, {SLUG_PREFIX}-02, etc.
